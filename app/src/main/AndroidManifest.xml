<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ananananzhuo.roomdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoomDemo">
        <activity
            android:name=".savelist.SaveListDataActivity"
            android:exported="true" />
        <activity android:name=".memorydatabasebuilder.MemoryDatabaseBuilderActivity" />
        <activity android:name=".operatewithlivedata.OperateWithLivedataActivity" />
        <activity android:name=".simpleuse.SimpleUseActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".relative.OneToOneActivity"/>
        <activity android:name=".relative.OneToManyActivity"/>
        <activity android:name=".relative.ManyToManyActivity"/>
        <activity android:name=".embdded.EmbddedActivity"/>
    </application>

</manifest>